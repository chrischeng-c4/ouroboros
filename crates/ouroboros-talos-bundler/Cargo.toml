[package]
name = "ouroboros-talos-bundler"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# Workspace dependencies
anyhow = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
dashmap = { workspace = true }
parking_lot = { workspace = true }
rayon = { workspace = true }

# Graph dependencies
petgraph = { workspace = true }

# AST parsing (same versions as transform crate)
tree-sitter = "0.24"
tree-sitter-javascript = "0.23"
tree-sitter-typescript = "0.23"

# Talos internal crates
ouroboros-talos-resolver = { path = "../ouroboros-talos-resolver" }
ouroboros-talos-transform = { path = "../ouroboros-talos-transform" }
ouroboros-talos-asset = { path = "../ouroboros-talos-asset" }

[dev-dependencies]
tempfile = "3.15"
tracing-subscriber = { workspace = true }
