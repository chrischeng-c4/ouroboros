[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
celery = ">=5.3"
pydantic = {extras = ["email"], version = "^2.0"}
fastapi = ">=0.100"

[dev-packages]
pytest = ">=7.0"
mypy = "*"

[requires]
python_version = "3.10"
